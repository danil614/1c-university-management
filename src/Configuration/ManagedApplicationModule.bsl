Процедура ПриНачалеРаботыСистемы()
	МассивСтрок = ПроверкаДнейРожденияВызовСервера.ПолучитьСтрокиСообщенияДниРожденияСегодня();
	СтрокаСообщения = СтрСоединить(МассивСтрок, Символы.ПС);
	Если Не ПустаяСтрока(СтрокаСообщения) Тогда
		Сообщение = Новый СообщениеПользователю;
		Сообщение.Текст = "Сегодня день рождения:" + Символы.ПС + СтрокаСообщения;
		Сообщение.Сообщить();
	КонецЕсли;
КонецПроцедуры